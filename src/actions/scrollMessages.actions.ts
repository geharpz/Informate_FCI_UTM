export class ScrollMessagesActions{
/*
    const options = {
      root: document.querySelector('.contM'),
      rootMargin: '0px',
      threshold: 0.1
    }
    const intersectionTopObserver:any=new IntersectionObserver((entries)=>{
      const ratio=entries[0].intersectionRatio;
      if(ratio>0 && this.state.skip!=0){

        if((this.state.skip-50)<0){
          this.setState({skip:0});
        }else{
          this.setState({skip:this.state.skip-50});
        }

     this.messageActions.fetchGetMessages(this.state,
          this.setState.bind(this),
          this.props.id_grupo,this.state.skip);
          this.contScrollRef.current.scrollTop=20;
      }
    },options);

    const intersectionBottomObserver:any=new IntersectionObserver((entries)=>{
      const ratio=entries[0].intersectionRatio;
      if(ratio> 0 &&
        this.state.skip <this.state.maxFile){

        if ((this.state.skip + 50) >= this.state.maxFile) {
         this.setState({ skip: this.state.maxFile });
          return;
         
        } else {
        this.setState({ skip: this.state.skip + 50 });
         
        }
   
   //  this.contScrollRef.current.scrollTop=scrollTop-20;
     this.messageActions.fetchGetMessages(this.state,
          this.setState.bind(this),
          this.props.id_grupo,this.state.skip);
          this.contScrollRef.current.scrollTop=20;
      }
    },options);

    intersectionTopObserver.observe(this.contScrollTopRef.current);
    intersectionBottomObserver.observe(this.contScrollBottomRef.current);
    */
}